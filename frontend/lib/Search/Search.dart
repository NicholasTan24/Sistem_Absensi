import 'package:flutter/material.dart';

class SearchKaryawan extends StatelessWidget {
  const SearchKaryawan({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
